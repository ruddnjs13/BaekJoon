#include <iostream>
#include <string>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    string input;
    cin >> input;

    cout << ":fan::fan::fan:\n";
    cout << ":fan::" << input << "::fan:\n";
    cout << ":fan::fan::fan:";
}
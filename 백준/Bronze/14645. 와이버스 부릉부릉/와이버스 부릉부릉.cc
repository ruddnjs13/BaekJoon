#include<iostream>
using namespace std;



int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int n, k, a, b;
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		cin >> a >> b;
	}

	cout << "비와이";
}
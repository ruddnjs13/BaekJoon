#include <iostream>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;

    int a, b;

    cin >> t;

    for (int i = 0; i < t; i++) {
        cin >> a >> b;
        cout << "yes" << '\n';
    }
}